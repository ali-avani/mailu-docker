# Installer

Bash script to install and config mailu server.

## Installation

Clone the scripts:

```
git clone https://github.com/ali-avani/mailu-docker.git
cd mailu-docker
```

Edit configuration:

```
cp .env.sample .env
vim .env
```

Run the installer:

```
bash install.sh
```
